# محرك البحث «وكيل أسامة» — خطة P11 (وثيقة خطّة)

> حالة الوثيقة: **خطة موثّقة، جاهزة للتنفيذ** (لم تُنفَّذ بعد).
> هذه الوثيقة مصدر الحقيقة لمرحلة P11. عند التنفيذ يُحدَّث قسم «سجل التنفيذ» والجداول أدناه
> خطوة بخطوة، حتى يكون كل ما تم فعله موثّقًا.

---

## 1. الهدف

1. **شريط البحث في الرئيسية** (`app/(auth)/(tabs)/index.tsx` ← `⚠ /search`) يصبح **متصفِّح ويب**:
   عند وجود اتصال بالإنترنت تكون النتائج **من الويب (صفحات)** تفتح داخل **متصفِّح WebView داخلي**.
   وفي **وضع عدم الاتصال يبقى البحث المحلي كما هو تمامًا الآن** (بلا أي تغيير).
2. **FLOW (ريلز أونلاين)** يبقى باحثًا عن **الفيديوهات القصيرة في يوتيوب فقط** بسلوكه الحالي غير المتغيّر،
   مع **بنية جاهزة للتوسّع** إلى إنستغرام وتيك توك لاحقًا (موثّقة أدناه، **بلا تنفيذ الآن**).
3. **مستقليَّة صارمة**: الباحث لا يمسّ عمل وكيل أسامة، ولا مفاتيحه، ولا اتصاله، ولا خادمه، ولا بوابته الصوتية.
   ملفات النظام تُقرأ فقط ولا تُعدَّل.

## 2. القرارات المعتمدة (أُخذت من هذا المستخدم explicitly)

| القرار | الاختيار | الأثر |
|---|---|---|
| إنستغرام/تيك توك الآن؟ | **يوتيوب فقط**؛ تفعيل IG/TikTok لاحقًا | لا خادم مضيف جديد الآن؛ الواجهة جاهزة بموفِّد `provider` |
| فتح نتائج الويب؟ | **متصفِّح WebView داخلي** (modal جديد) | ملف `app/(auth)/(modal)/browser/[url].tsx` + تسجيل مسار |
| الوضع الافتراضي؟ | **أونلاين = ويب**، **أوفلاين = محلي** (يبقى الوضع الحالي أوفلاين) | اعتماد `useOnline` عند تهيئة الشاشة فقط |

## 3. البنية الحالية (نتيجة الفحص الفعلي)

| المكوّن | الموقع | الوصف الحالي |
|---|---|---|
| شريط البحث | `app/(auth)/(tabs)/index.tsx:172` | `router.push('/search')` |
| شاشة البحث | `components/search/SearchScreen.tsx` | وضعان: `local` (بحث في كل ما هو مخزَّن على الجهاز) و`flow` (بحث يوتيوب قصير عبر `searchShorts`) |
| محرّك البحث المحلي | `utils/search/searchLocal.ts` | قراءة SQLite للدردشات/الفيديوهات/البرومبتات (لا يُعدَّل) |
| نظام المفاتيح | `utils/apiHub/` | سلسلة مفاتيح يوتيوب (`getYtKeyEntries`) + `runWithFailover` (rotation + cooldown) + سجلّ مزوّدين `CATALOG` (فئات `search`/`social`) + سجلّ مثيلات Piped/Invidious |
| عميل يوتيوب | `utils/flow/youtubeClient.ts` | `searchShorts`/`enrichDuration`/`fetchFeedPage` عبر apiHub (لا يُعدَّل) |
| وكل أسامة | `utils/OpenCodeAgent.ts` + `utils/Opencode.ts` + `server/voice_gateway.py` (8100) | مفاتيح zen المدمجة + خادم opencode LAN (4096) + بوابة صوت — **خارج نطاق التعديل نهائيًا** |
| أدوات متوفرة | `react-native-webview@13.15.0`، `expo-linking`، `useOnline` في `utils/flow/net.ts`، `tests/helpers.ts` | تُستخدم في المرحلة |

## 4. التصميم — الملفات الجديدة (مضافة فقط)

| الملف | الدور |
|---|---|
| `utils/search/webSearch.ts` | محرّك بحث ويب **بدون مفاتيح** (كل المزوّدين `auth:none` وفق كتالوج apiHub). سلسلة failover عبر `runWithFailover` من apiHub (قراءة): DuckDuckGo Lite ثم Wikipedia. توابع تنقية نقية: |
| `utils/search/socialSearch.ts` | `searchSocial(provider, query, lang)`: `youtube` → يلفّ `searchShorts` الحالي كما هو؛ `instagram`/`tiktok` → واجهة جاهزة تعيد `providerUnavailable: true` |
| `app/(auth)/(modal)/browser/[url].tsx` | متصفِّح WebView داخلي (نمط `(modal)/pdf/[uri].tsx`): شريط إغلاق/عنوان/فتح خارجي (`Linking`) عند فشل التحميل |
| `tests/webSearch.test.ts` | اختبارات Node-safe للتوابع النقية (ذات نمط `tests/helpers.ts`) |
| `tests/socialSearch.test.ts` | اختبارات Node-safe لواجهة socialSearch + سلوك youtube |

## 5. التصميم — الملفات المعدَّلة (أدنى حد، إضافة آمنة فقط)

| الملف | ما يُضاف | ما لا يُمس |
|---|---|---|
| `components/search/SearchScreen.tsx` | حقن `useOnline`؛ ثلاثي أوضاع **ويب \| ريلز \| محلي**؛ الافتراضي أونلاين=ويب وأوفلاين=محلي (تهيئة لمرة واحدة، يُحترَم اختيار المستخدم بعدها)؛ بطاقات نتائج ويب (مضيف+عنوان+مقتطف) تفتح متصفِّح WebView؛ رسالة أوفلاين ناعمة لوضعي الويب/الريلز ولا كسر للبحث المحلي | منطق `local` و`flow` الحالي وتبويب الواجهة وdebounce 280ms |
| `i18n/strings.ts` | مفاتيح `search` جديدة فقط (عربي/إنجليزي): `web`, `openInBrowser`, `offline`, `unavailableInstagram`, `unavailableTiktok`, تسمية أوضاع | كل المفاتيح القائمة حرفيًا |
| `app/(auth)/_layout.tsx` | تسجيل `(modal)/browser/[url]` فقط | باقي الشاشات |

## 6. كيفية استخدام «نظام المفاتيح» بذكاء (دون كسر)

- **ويب**: صفر مفاتيح — DuckDuckGo Lite وWikipedia كلاهما `auth: none` في كتالوج apiHub؛ لا إرسال لأي مفتاح، لا طلب لخادم وكيل أسامة.
- **يوتيوب**: يعيد استخدام **نفس سلسلة مفاتيح YT** الموجودة (`ytApiKey`/`ytApiKeys` في `app.json` + مفاتيح المستخدم) عبر `getYtKeyEntries` و`runWithFailover` **بدون تعديل apiHub**؛ سلوك مماثل تمامًا للوضع الحالي.
- **لا تُكتب مفاتيح**، لا تُعدَّل مهلات، لا تُغيَّر cooldown، لا لمس `OpenCodeAgent`/`Opencode`/`voice_gateway.py`.

## 7. الشروط الصارمة (غير قابلة للتفاوض)

1. الوضع الأوفلاين الافتراضي = **بحث محلي ثابت السلوك**؛ لا يقفز للويب تلقائيًا.
2. الوضع الأونلاين الافتراضي = **ويب**؛ يبقى «الريلز أونلاين» والمحلي قابلين للاختيار يدويًا.
3. أي فشل (شبكة/مزوّد/تحليل) → **حالة خطأ ناعمة** بنمط `search.error` الحالي؛ لا استثناء غير مُلتقَط خارجة.
4. مهلة لكل طلب ويب = **8 ثوانٍ** عبر `AbortController`؛ debounce **280ms**.
5. نحن لا نعدِّل: `utils/apiHub/*`, `utils/OpenCodeAgent.ts`, `utils/Opencode.ts`, `utils/voice/*`, `utils/flow/youtubeClient.ts`, `FlowScreen.tsx`, `server/*`, الدردشة، ومجموعتا الاختبار `documentSchema`/`longDocument` الفاشلتان (عمل موازٍ خارج النطاق؛ تُسجَّل حالة «قبل/بعد» فقط في تقرير التحقق).
6. صفر اعتماديات جديدة (لا `npm install` جديد) — `react-native-webview` موجود مسبقًا.

## 8. سيناريوهات السلوك

| السيناريو | السلوك المتوقَّع |
|---|---|
| أونلاين + فتح البحث | الوضع التلقائي = ويب؛ كتابة استعلام → نتائج صفحة → فتحها في WebView |
| أوفلاين + فتح البحث | الوضع التلقائي = محلي (كما اليوم)؛ اختيار ويب/ريلز يدويًا → رسالة «لا اتصال» |
| طلب ويب فشل من جهة DDG | failover تلقائي إلى Wikipedia ← إن لم ينجح أي مصدر → خطأ ناعم |
| نتيجة ويب يوتيوب | تفتح في **المتصفِّح** (وليس مشغّلاً في FLOW) |
| فيديو من «ريلز أونلاين» | يفتح في مشغِّل FLOW الحالي كما اليوم (لا تغيير) |
| اختيار إنستغرام/تيك توك | `providerUnavailable` → رسالة «متاح قريبًا»، لا كسر |

## 9. محطة IG/TikTok لاحقًا (موثَّقة فقط، غير منفَّذة)

سطر واحد عندما يُفعَّل: باحث يوتيوب/إنستغرام/تيك توك عبر **خدمة مضيفة مستقلة** (`server/searcher.py`)
على منفذ جديد **8300** تستخدم `yt-dlp` المثبَّت على المضيف. شرطان صارمان:
- **مستقلة كليًا** عن بوابة الصوت (8100) وخادم opencode (4096) — لا تعديل لهما؛
- `socialSearch.instagram/tiktok` يشغّلان طلبًا إلى `http://<MP-المضيف>:8300/search/{tiktok|instagram}` ويعيشان إخفاقهما بنعومة.
لماذا: تيك توك وإنستغرام لا يسمحان ببحث مباشر من التطبيق بدون OAuth؛ yt-dlp الطريق الواقعي.

## 10. الاختبار والتحقّق

1. `npm run typecheck` → **PASS**.
2. `npm test` → مجموعتا `webSearch` و`socialSearch` الجديدتان **PASS**؛ الفشلان المعروفان (`documentSchema`/`longDocument`) ثابتان قبل وبعد دون لمس.
3. (اختياري) `npx expo export --platform android` → **PASS**.
4. فحص يدوي عبر Expo Go على نفس شبكة المضيف.

## 11. التراجع

- حذف الملفات الجديدة الأربعة + عودة `SearchScreen.tsx`/`i18n/strings.ts`/`_layout.tsx` من محفوظات git بشرط ألا تكون ثمّت تعديلات أخرى عليها بينهما.

## 12. سجلّ التنفيذ

| الخطوة | الحالة | ملاحظات |
|---|---|---|
| 1. `utils/search/webSearch.ts` + اختباره | ⏳ معلَّق | — |
| 2. `utils/search/socialSearch.ts` + اختباره | ⏳ معلَّق | — |
| 3. `(modal)/browser/[url].tsx` + التسجيل + i18n | ⏳ معلَّق | — |
| 4. دمج الأوضاع الثلاثة في `SearchScreen.tsx` | ⏳ معلَّق | — |
| 5. typecheck + test + توثيق نهائي | ⏳ معلَّق | — |